import { Handler } from './handler';
import { Config } from '../config/config';

export class CreditPatternHandler implements Handler{
  public initMap: string;
  public handleInput(input: string): string {
    return "";
  }
}
